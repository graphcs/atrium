{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @atrium/frontend build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
